# Configuración de la base de datos
# Se pueden sobrescribir mediante variables de entorno:
#   DB_URL, DB_USERNAME, DB_PASSWORD, DB_POOL_SIZE
#
db.url=jdbc:mysql://localhost:3306/electrosur_jvl
# Usuario y contraseña se externalizan; dejar valores vacíos si se usará solo mediante variables de entorno
db.username=
db.password=
db.pool.size=10
